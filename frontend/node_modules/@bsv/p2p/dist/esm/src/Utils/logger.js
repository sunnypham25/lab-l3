export class Logger {
    static isEnabled = false;
    static enable() {
        this.isEnabled = true;
    }
    static disable() {
        this.isEnabled = false;
    }
    static log(...args) {
        if (this.isEnabled) {
            console.log(...args);
        }
    }
    static warn(...args) {
        if (this.isEnabled) {
            console.warn(...args);
        }
    }
    static error(...args) {
        console.error(...args);
    }
}
//# sourceMappingURL=logger.js.map